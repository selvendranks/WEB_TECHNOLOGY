<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <form action="/products/<%= foundProduct._id %>?_method=PUT" method="POST">
        <label for="name">Name </label>
        <input type="text" name="name" placeholder="productname" id="name" value="<%= foundProduct.name %>">

        <label for="price">Price</label>
        <input type="number" name="price" placeholder="product price" id="price" value="<%= foundProduct.price %>">

        <label for="category">Category</label>
        <select name="category" id="category">
            <option value="fruit" <%= foundProduct.category === 'fruit'? 'selected':'' %> >Fruit</option>
            <option value="vegetable" <%= foundProduct.category === 'vegetable'? 'selected':'' %>>Vegetable</option>
            <option value="dairy" <%= foundProduct.category === 'dairy'? 'selected':'' %>>Dairy</option>
        </select>
        <button>Submit</button>
    </form>
    <a href="/products/<%= foundProduct.id %>">Cancel</a>
</body>
</html>